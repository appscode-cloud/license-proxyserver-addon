# license-proxyserver-addon

```
kubebuilder init --domain open-cluster-management.io --skip-go-version-check
kubebuilder create api --group licenses.appscode.com --version v1alpha1 --kind LicenseProxyServerConfig
```